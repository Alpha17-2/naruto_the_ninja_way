import 'package:flutter/material.dart';
class spiritScreen extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Spirit')),
    );
  }
}